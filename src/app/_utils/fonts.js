import { Inter, Anton, Hanken_Grotesk } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const anton = Anton({ subsets: ["latin"], weight: ["400"] });
export const hanken = Hanken_Grotesk({
  subsets: ["latin"],
});
